from setuptools import setup, find_packages

setup(
    name='cloud_tracking_tools',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # List your package dependencies here
    ],
)